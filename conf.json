{
    "foo": "bar",

    "mongo" :
    {
        "useAuth" : true,
        "host": "localhost",
        "port":27017,
        "username" : "default",
        "password" : "default",
        "dbname": "CAI_webProject",
        "staticImgs": "./src/main/resources/web/beers/img",
                                                                    /* Mongo embedded part */
                                                                    //port: The port Mongo listens on (defaults to 27017)
                                                                    //bindIp: The ip address Mongo binds itself to (defaults to '127.0.0.1')
        "logging" :  "console",                                     ///The type of logging to be produced: 'console', 'file' or 'none' (defaults to 'file')
        "logFilePath" : "embedded-mongo.log",                       //logFilePath: The desired log file path (defaults to 'embedded-mongo.log')
        "version" : "PRODUCTION",                                   //version: The version of Mongo to run. Can be 'DEVELOPMENT' or 'PRODUCTION' for the latest versions of the respective branch, or take the form '1.2-LATEST' or '1.2.3' for specific versions
        "storageLocation" : "./"                                    //storageLocation: The directory location from where embedded Mongo will run, such as /tmp/storage (defaults to a java temp directory)
    }
    

}